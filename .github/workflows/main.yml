name: Main branch work flow

on:
  push: 
  branches:
     - main

jobs:
  build-main:
     runs-on: ubuntu-latest

 steps:
      -name: checkout code   
      uses: actions/checkout@3
      -name: run a script
      run: echo "this run only on the main branch"
